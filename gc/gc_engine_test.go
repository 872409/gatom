package gc

import (
	"testing"
)

// func wrap(fn func(gc *GContext)) gin.HandlerFunc {
// 	return func(ctx *gin.Context) {
// 		fn(New(ctx))
// 	}
// }

func Test_engine(t *testing.T) {

	// engine := NewEngine()
	// engine.GET("/", wrap(func(gc *GContext) {
	//
	// }))
	// log.Infoln(engine)
}
